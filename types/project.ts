export interface Project {
    year: number;
    month: string;
    description: string;
    location: string;
    currency: string;
    amount: number;
}

export interface ProjectCategory {
    name: string;
    value: number;
    color: string;
}

